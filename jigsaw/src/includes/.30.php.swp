<?php
$servername = "localhost";
$username = "SIben";
$password = "*****************************";

// Create connection
$conn = new mysqli($servername, $username, $password);

// Check connection
if ($conn->connect_error) {
    die("Generated error:" . $conn->connect_error);
}

die($password);

?>
